- recuperer l'ip de la machine qui heberge genre 10.2.1.8 (commande [ifconfig | grep "inet 10."] pour avoir directement la bonne ligne)
- aller sur le profil de l'Intra
- aller dans settings de l'Intra
- cliquer sur api
- cliquer sur register a new app
- remplir les champ obligatoires
Dans Redirect URI ecrire : https://LIP_PUBLIC_DU_PC_QUI_HEBERGE:5000/api/auth/42/callback
bien evidement remplacer LIP_PUBLIC_DU_PC_QUI_HEBERGE par l'ip de la machine qui heberge genre 10.2.1.8
- appuyer sur submit.

dans l'app dans le dossier srcs creer un .prod.env (ou .dev.env selon le cas) qui ne dois pas exister car comme on est des bon dev, on l'a ajoute dans le gitignore bien evidement.

dans le .prod.env (ou .dev.env), completer les lignes suivantes :

FORTYTWO_CLIENT_SECRET="SECRET_OBTENU_SUR_L_INTRA"
VITE_42_CLIENT_ID="UID_OBTENU_SUR_L_INTRA"
VITE_42_REDIRECT_URI="https://LIP_PUBLIC_DU_PC_QUI_HEBERGE:5000/api/auth/42/callback" ## L'URL exacte dans le back-end

Bien evidement remplacer par les bonnes Info !!!!

make prod et prier !!!!!!!
